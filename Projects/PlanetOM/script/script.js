const typinText = document.getElementById("hmm");
const struggleCard = document.getElementById("struggle-card");

typinText.addEventListener('click', () => {
    window.location.href = "#autot-card";

});